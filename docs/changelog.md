# 🍁枫叶指令宏

 🚀 **枫叶指令宏——让自动化变得简单！无需编程基础，通过简单指令即可实现自动化操作。**

---


如有可能,请更新到最新版本.
如有BUG.请反馈.

---

5.7说明:

        修复了循环BUG(循环不独立计数问题).
        修复了模块定时功能的BUG(不能正确退出定时).



5.6说明:

        加强 大模型文字识别 指令.
        现在会保存识别范围内的所有字段的坐标.
        完整指令参数.
        大模型文字识别\330,524,510,692,识别结果,字段数量,文字X,文字Y\

5.5说明:

        后台加强版.
        新增以及优化了一些后台功能.

        @调用符号 增强.
        可先拼接再调用.
        示例:
        变量\变量名1,100\
        变量\次数,1\
        弹窗提示\@变量名1\
        弹窗提示\@(变量名.1)\
        弹窗提示\@(变量名.@次数)\
        
        新增 字符功能. <坐标提取>
        坐标提取\@识别结果,坐标X,坐标Y\        
        
        新增 图形预览器 功能.
        现在可以预览图像转文本数据的原图形.

5.4说明:

        新增 模块定时 功能

        此功能可以定时调用模块. 从现在起多久后执行(启动时不调用,设置的时间到才调用).
        不管主线程在干什么.都会执行模块里的指令.
        直到主线程结束才结束(如果不关闭的话).

        新增 变量监控 窗口  : 可在日志里展开查看变量变化
        新增 坐标提示 指令  : 坐标提示\500,500\

        新增 AI 助手

        迎合人工智能的需求,语法必须规严谨范化.
        判断类型 移除难理解的判断.  变量. 判断窗口
        识图参数新增.  识图数量  识图X  识图Y  按秒等待

        识图完整指令参数:
        识图\0,0,0,0,0.1,<数据名>,标记结果,识图数量,识图X,识图Y,等待出现,3\
        识图等待模式可以按秒等待.

5.3说明:

        添加鼠标滚轮功能

        新增鼠标滚轮事件监听
        现在录制功能可以监听鼠标滚轮事件
        
        OCR大模型现在会自动下载.

        修复运行日志画面抖动问题.  

5.2说明:

        识图功能:
        识图到的数量存放到固定变量  <数据名>数量
        识图到的第一个坐标：<数据名>1X  <数据名>1Y
        识图到的第二个坐标：<数据名>2X  <数据名>2Y
        坐标依此类推，       

        关于打包:
        软件说明-显示问题修复.
        软件说明(脚本打包后软件能看到这个脚本的使用说明)现在每个项目独立一个.
        启动快捷键 和 宏启动快捷键 两个都不设置的话(留空)。
        打包后的脚本运行会自动启动(适用开机自启动脚本)。

5.1说明:

        高级功能 新增指令
        新增 抓图 指令.
        抓图\0,0,100,100,数据名,灰度阈值,150\
        新增  截图 指令.
        截图\0,0,0,0,截图名\


5.0说明:

        优化 图片转文本数据.
        现在每个项目独立一个 txt文本数据
        txt文本数据每个项目下会独立创建一个.

        关于 打包功能配置存放.
        txt文本数据 依然在config目录里.OCR模型也在config目录里.
        (如果有配置文件txt数据.则优先使用.否则使用内部txt数据)

        优化 项目 新增删除改名载入.
        
        优化 保存
        
        优化  获取时间 指令.
        现在变量不固定.能自由更改.
        获取时间\年,月,日,时,分,秒\
        获取时间\年,月,日,小时,分钟,秒\
        如果\比较\@分,等于,@分钟,比较结果\
        成功\弹窗提示\时间相同\
        两个时间对比直接更换变量接收直接比较就可以了.
        
        
        高级功能 新增  关机操作  指令.
        关机操作\正常关机       
        关机操作\强制关机     
        

       
4.9说明:

        新增 运行日志
        现在运行的时候能查看指令运行到了什么位置

        循环指令优化
        如果循环次数给的是变量
        变量在循环里变更次数后循环次数也会变更

        因为循环底层的修改
        现在退回老版本的循环指令逻辑.
        返回循环\
        终止循环\
        现在不能追加到行尾.
        必须独立再开一行.

        快捷键优化
        
        新增 停止功能 
        现在运行的时候可以停止(结束当前运行的指令).
        循环中也可以停止.

        新增 暂停功能 
        现在运行的时候可以暂停(暂停当前运行的指令).
        再按一次暂停键可以恢复运行(从暂停的位置开始运行).

        现在重新排序快捷键
			
4.8说明:     

        添加全屏抓图 快捷键
        修复了快捷键设置为空不能保存问题
        修复了快捷键设置侧键长按无效问题
        修复了后台绑定识图运行结束后不自动解绑问题(导致抓图只能抓绑定的窗口)

4.7说明:

        优化录制功能 
        现在能准确监听鼠标事件

4.6说明:

        杂项 改为 设置。
        新增项目功能
        可以设置多个项目，每个项目独立保存。
        一个项目 10个配置页 + 一个模块库。
        新增宏(长按)快捷键,可以长按一直触发指令宏
        现在可以单独设置每个配置页的快捷键了。
        键盘鼠标组合键说明已经添加。
        			

4.5说明: 

        功能选项更新.
        新增窗口功能项.
        整合之前的有关窗口的功能项.

        窗口位置大小 更名为 获取窗口信息

        更改窗口 指令 把原先三个功能拆分出来了.
        移动窗口 
        更改窗口大小
        更改窗口标题

        新增  更改窗口透明
        
4.4说明:

        优化循环指令逻辑.

        返回循环
        终止循环
        现在可以跟在行尾

        录制功能上线.

4.3说明:
        
        修复配置文件BUG.

4.2说明:

        现在打包更加个性化了.


4.1说明:

        过渡版本.

        优化了指令的数据,现在会保存在配置目录里.

        优化多项指令.不能传参问题.

        更改指令新增更改标题(查看更改窗口说明).

        备注:如果之前写的指令数据后续需要使用的话.这个版本必须运行一次.
        后续版本数据会一直使用配置目录里的数据(这个版本不运行的话,数据会丢失).


4.0说明:

        新增 文本播报 指令.

        文本 指令 更名为 文本输入

3.9说明:

        增强写指令的便捷性.现在可以在光标处插入指令.

        取色 指令 更改为 坐标找色

        新增 范围找色 指令
        
        新增 多项指令 说明


3.8说明:

        取色功能 新增 
        现在获取色值的同时可直接获取窗口名(鼠标下方的窗口)
        以及窗口高宽还有范围(左上角坐标和右下角坐标)
        现在获取到的坐标自动传入 鼠标移动参数里.

        新增 获取偏移 功能.
        自动计算鼠标移动的偏移值.


3.7说明:

        取色功能 新增 
        现在获取色值的同时可直接获取窗口名(鼠标下方的窗口)

        按键功能 新增.
        按键键位现在可以接收变量

        更新热键功能(支持热键保存).
        快捷键现在可以保存.支持键鼠启动(查看特殊按键).

        修复延迟指令BUG问题.

        新增  模块库名显示

        新增  特殊按键显示

3.6说明:

        新增 功能选项-字符功能

        字符串长度 指令
        能获取字符串的长度

        检索字符串 指令
        能检索字符串的指定位置字符

3.5说明:

        UI 缩放有BUG. 现在改回固定不可缩放.

        功能选项更新.

        部分添加说明.

        部分指令更改指令名.
        光学字符识别 指令更改 为 大模型文字识别

        运算指令添加取余数功能.

        优化部分代码.


3.4说明:

        判断窗口 指令新增激活模式
        
        判断窗口是否存在 (可写可不写) 示例:

        如果\判断窗口\QQ\
        成功\弹窗提示\成功\
        失败\弹窗提示\失败\

        如果\判断窗口\QQ,存在\
        成功\弹窗提示\成功\
        失败\弹窗提示\失败\

        判断窗口是否激活 示例:

        如果\判断窗口\QQ,激活\
        成功\弹窗提示\成功\
        失败\弹窗提示\失败\


3.3说明:

        修改了 <比较>  字符包含 的返回值 比较结果
        之前返回1或者0.表示真或者假.
        (别的比较指令依然返回1或者0)
        现在 字符包含 直接返回包含多少个.
        
        新增  <提取字符>  指令.  示例:

        变量\变量名,abc123\

        提取字符\@变量名,英文字母,英文\
        提取字符\@变量名,数字字符,数字\
        弹窗提示\@英文       @数字\

        提取字符\abc123中文,中文字符,提取结果\
        弹窗提示\@提取结果\


                
3.2说明:
                
        打包功能现在不需要依赖配置文件.直接用exe文件运行即可.
        (打包后直接发送exe文件给别人就能使用.不依赖txt数据)
        
        如果有配置文件txt数据.则优先使用.否则使用内部txt数据

        OCR模型依然需要外部配置文件.				
				
3.1说明:
                
        新增 打包功能(编译exe文件)配置文件相同
        打包后的exe是独立文件发给别人就可以直接运行
        (但需要携带config文件夹)

        修改路径config文件夹存放位置
        文本数据,图标,以及RapidOcr存放
        要打包给别人的话直接发送打包好的exe和config文件夹

        图标是ico格式(如果要换图标,请自行修改,名字一样就行.)
		
3.0说明:

        新增 比较功能(字符包含) 判断字符是否包含
        使用示例:
        
        如果\比较\枫叶指令宏,字符包含,指令,比较结果\
        成功\弹窗提示\成功\
        失败\弹窗提示\失败\

        如果\比较\123,字符包含,1,比较结果\
        成功\弹窗提示\成功\
        失败\弹窗提示\失败\
        
        如果\比较\qwer,字符包含,we,比较结果\
        成功\弹窗提示\成功\
        失败\弹窗提示\失败\

2.9说明:

        新增OCR模型(光学字符识别) 
        如需OCR模型功能需要添加模型文件(解压放在软件目录下)
        
        使用示例:

        光学字符识别\907,622,1357,721,识别结果\

        弹窗提示\@识别结果\	



2.8说明:

        <移动窗口>指令更改为:更改窗口
        
        ;移动窗口
        更改窗口\微信,0,0\

        ;修改窗口大小(宽高)
        更改窗口\微信,,,500,500\

        ;移动并且修改窗口大小(宽高)
        更改窗口\微信,0,0,1000,1000\
				
				
2.7说明:
                
        新增获取时间功能(本地时间).

        获取时间\@年,@月,@日,@时,@分,@秒\

        弹窗提示\@年 年 @月 月 @日 日 @时 时 @分 分 @秒 秒\


        
2.6说明:
                
        修复延迟接收变量问题.

2.5说明:
                
        新增UI缩放功能.大小调节.

        			
2.4说明:

        新增功能选项:获取窗口名的指令(自动保存到剪切板,获取后直接粘贴就行).

        获取窗口名\窗口名结果\

        弹窗提示\@窗口名结果\


2.3说明:

        修复数据莫名丢失问题.

        新增模块配置存放(每次运行都参与解析).			


2.2说明:

        修复已知BUG.	


2.1说明:

        新增后台鼠标按下抬起(支持变量)

        后台使用案例:

        鼠标\左键按下,193,132\

        鼠标\左键抬起,56,132\